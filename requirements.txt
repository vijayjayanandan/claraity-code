# Core Dependencies - Tested & Working Versions
# transformers>=4.40.0  # REMOVED: Using LLM APIs instead of local models
# sentence-transformers>=2.7.0  # REMOVED: Using Alibaba embeddings API (Option B architecture)
chromadb>=0.4.24  # Keep for local vector DB (fast queries)
tiktoken>=0.7.0

# AST Parsing - Specific versions required for compatibility
tree-sitter==0.25.2  # Required: v0.25+ has breaking changes
tree-sitter-languages>=1.10.2


# SQLite upgrade for ChromaDB compatibility
pysqlite3-binary

# HuggingFace fast downloads
hf-transfer

# Data Models & Validation
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Web Framework (optional - for future API)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
websockets>=12.0
beautifulsoup4>=4.12.0  # HTML text extraction for web_fetch tool

# Async I/O
aiofiles>=23.2.1

# Caching & Storage (optional)
redis>=5.0.0
sqlalchemy>=2.0.30
alembic>=1.13.0

# Document Reading (PDF, Word)
PyMuPDF>=1.23.0  # PDF text + table extraction (import as fitz)
python-docx>=1.0.0  # Word .docx paragraph + table extraction

# CLI & UI
rich>=13.7.0
prompt_toolkit>=3.0.0  # Interactive CLI with autocomplete and key bindings
typer>=0.12.0
pygments>=2.20.0  # Syntax highlighting for code display
colorama>=0.4.6  # Cross-platform colored terminal text (Windows ANSI support)
pyperclip>=1.8.0  # Cross-platform clipboard copy/paste
Pillow>=10.0.0  # Image processing for screenshot clipboard support

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.1
jinja2>=3.1.4
pathspec>=0.9.0  # Gitignore-style pattern matching for .claraityignore

# Git Integration (optional)
gitpython>=3.1.43

# File Watching (optional)
watchdog>=4.0.0

# System Monitoring (optional)
psutil>=5.9.8

# LLM Backends (install as needed)
# ollama  # Use official Ollama installation instead
# requests>=2.31.0  # For Ollama API calls (typically already installed)
openai>=1.30.0  # For OpenAI-compatible API backends (OpenAI, Alibaba Cloud, Azure, Groq, etc.)

# Observability (LLM tracing, metrics, cost tracking)
langfuse>=2.0.0  # Production-grade LLM observability platform
structlog>=24.1.0  # Structured logging with context binding

# Note: For GPU acceleration, install Ollama on your system
# CPU: Works but slow (30-60s per query)
# GPU: 10-30x faster (1-5s per query) - See RUNPOD_QUICKSTART.md
